{% extends 'templates/app.twig' %}

{% block content %}
   {% for post in posts %}
      <div class="post">
         {{ post.title }}
      </div>
   {% endfor %}

   {{ posts.links | raw }}
{% endblock %}